---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { inspectionScopeMailtoHref } from "../../lib/contact";
import { getServicePageStructuredData } from "../../lib/structuredData";

const title = "Engagement Boundaries | Sayyidi Shaarani";
const description =
  "Professional boundaries for structured web application security inspections, including authorization, scope, and data handling.";

const mailtoHref = inspectionScopeMailtoHref;

const structuredData = getServicePageStructuredData({
  path: "/boundaries/",
  title,
  description,
});
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath="/boundaries/"
  structuredData={structuredData}
>
  <section class="hero" aria-labelledby="boundaries-title">
    <h1 id="boundaries-title">Engagement Boundaries</h1>
    <p class="lede">
      Inspections are conducted within documented authorization and agreed scope.
      These boundaries protect operational stability and reporting integrity.
    </p>

    <ul class="atGlance" aria-label="Boundary summary">
      <li>Written authorization before testing</li>
      <li>No activity outside approved scope</li>
      <li>Evidence handling limited to engagement needs</li>
    </ul>

    <nav class="sectionLinks" aria-label="Boundary sections">
      <a href="#authorization-title">Authorization</a>
      <a href="#scope-title">Scope</a>
      <a href="#data-title">Data Handling</a>
      <a href="#exclusions-title">Exclusions</a>
    </nav>
  </section>

  <section aria-labelledby="authorization-title">
    <h2 id="authorization-title">Authorization Requirements</h2>
    <ul>
      <li>Written authorization is required before testing begins.</li>
      <li>Scope owner and approval contact must be identified in advance.</li>
      <li>Testing windows are scheduled and documented.</li>
    </ul>
  </section>

  <section aria-labelledby="scope-title">
    <h2 id="scope-title">Scope Control</h2>
    <ul>
      <li>Only agreed targets are inspected.</li>
      <li>No activity is performed outside approved boundaries.</li>
      <li>Validation remains proportionate to the agreed objective.</li>
    </ul>
  </section>

  <section aria-labelledby="data-title">
    <h2 id="data-title">Data Handling and Confidentiality</h2>
    <ul>
      <li>Findings are documented only for reporting and remediation guidance.</li>
      <li>Collected evidence is handled with confidentiality.</li>
      <li>Retention is limited to engagement and reporting requirements.</li>
    </ul>
  </section>

  <section aria-labelledby="exclusions-title">
    <h2 id="exclusions-title">Exclusions</h2>
    <ul>
      <li>No adversarial simulation beyond agreed inspection boundaries.</li>
      <li>No regulatory attestation or certification claims.</li>
      <li>No unmanaged scanning across unknown third-party infrastructure.</li>
    </ul>
  </section>

  <section aria-labelledby="start-title">
    <h2 id="start-title">Start a Scoped Discussion</h2>
    <p>
      To begin, provide the target environment, short application context,
      preferred window, and the authorized contact for scope approval.
    </p>
    <div class="callout">
      <p><strong>Include:</strong></p>
      <ul>
        <li>Target URL or environment</li>
        <li>Short application description</li>
        <li>Preferred testing window</li>
        <li>Scope authorization contact</li>
      </ul>
    </div>
    <nav class="ctaRow" aria-label="Boundaries page actions">
      <a class="cta ctaPrimary" href={mailtoHref}>Discuss Inspection Scope</a>
      <a class="cta ctaSecondary" href="/inspection/">View Inspection Model</a>
    </nav>
  </section>
</BaseLayout>

<style>
  section {
    margin: var(--space-5) 0;
    max-width: 70ch;
  }

  .hero {
    margin-top: var(--space-3);
  }

  .lede {
    font-size: 1.08rem;
    line-height: 1.55;
    max-width: 64ch;
  }

  .atGlance {
    margin: var(--space-3) 0 var(--space-2);
    padding-left: 1.2rem;
  }

  .sectionLinks {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    font-size: 0.9rem;
  }

  .sectionLinks a {
    color: var(--muted);
    text-decoration: none;
  }

  .sectionLinks a:hover {
    color: var(--text);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  ul {
    padding-left: 1.2rem;
  }

  li {
    line-height: 1.55;
  }

  li + li {
    margin-top: 0.35rem;
  }

  .callout {
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: var(--space-2);
    background: var(--bg-soft);
    margin-top: var(--space-2);
  }

  .callout p {
    margin-bottom: 0.4rem;
  }

  @media (max-width: 640px) {
    section {
      margin: var(--space-4) 0;
    }

    .hero {
      margin-top: var(--space-2);
    }
  }
</style>
